import{test}from"node:test";import assert from"node:assert/strict";import{addBox,deleteBox,summarize,normalizeLabel,isKnownRoom,nextBoxNumber,ROOMS,MAX_LABEL_LENGTH}from"../logic.js";
test("normalizeLabel",()=>{assert.equal(normalizeLabel("  Kitchen  –  Fragile  "),"Kitchen – Fragile");assert.equal(normalizeLabel("\n\tBedroom\t"),"Bedroom");assert.equal(normalizeLabel(null),"");assert.equal(normalizeLabel(void 0),"")})
test("isKnownRoom",()=>{assert.equal(isKnownRoom("Kitchen"),true);assert.equal(isKnownRoom("Garage"),false)})
test("addBox validation",()=>{assert.equal(addBox([],"","Kitchen").ok,false);assert.match(addBox([],"","Kitchen").error,/Enter a label/);assert.equal(addBox([],"a".repeat(MAX_LABEL_LENGTH+1),"Kitchen").ok,false);assert.match(addBox([],"a".repeat(MAX_LABEL_LENGTH+1),"Kitchen").error,/characters or fewer/);assert.equal(addBox([],"Dishes","Garage").ok,false);assert.match(addBox([],"Dishes","Garage").error,/valid room/)})
test("addBox sequential numbering prepends",()=>{const f=addBox([],"Glasses","Kitchen");assert.equal(f.ok,true);assert.equal(f.state.length,1);assert.equal(f.box.boxNumber,1);const s=addBox(f.state,"Books","Bedroom");assert.equal(s.ok,true);assert.equal(s.state.length,2);assert.equal(s.box.boxNumber,2);assert.equal(s.state[0].id,s.box.id)})
test("boxNumber skips after delete",()=>{const b1=addBox([],"Box 1","Kitchen").state;const b2=addBox(b1,"Box 2","Kitchen").state;const b3=addBox(b2,"Box 3","Kitchen").state;assert.equal(nextBoxNumber(b3),4);const d=deleteBox(b3,b3.find(b=>b.boxNumber===2).id);assert.equal(d.length,2);assert.equal(addBox(d,"Box 4","Kitchen").box.boxNumber,4)})
test("deleteBox removes matching id, frozen",()=>{const b=addBox(addBox([],"A","Kitchen").state,"B","Bedroom").state;const r=deleteBox(b,b[0].id);assert.equal(r.length,1);assert.equal(r[0].id,b[1].id);assert.equal(Object.isFrozen(r),true)})
test("summarize",()=>{ROOMS.forEach(r=>assert.equal(summarize([])[r],0));const s=summarize([{id:"1",label:"A",room:"Kitchen",boxNumber:1},{id:"2",label:"B",room:"Kitchen",boxNumber:2},{id:"3",label:"C",room:"Bathroom",boxNumber:3}]);assert.equal(s.Kitchen,2);assert.equal(s.Bathroom,1);assert.equal(s.Bedroom,0)})